// Print to the console: "Hello, Bob! In case you forgot, 3 x 4 is 12." Use variables for your name, a, b, and result. Also, try to it with string interpolation

const name = "Bob";
const a    = 3;
const b    = 4;
const result = a * b;

console.log(`Hello ${name}! In case you forgot, ${a} x ${b} is ${result}`);
