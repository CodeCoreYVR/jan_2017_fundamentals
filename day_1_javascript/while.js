// to construct a while loop you will need
// the `while` statement
// an expression that should give true / false
// a mechanism that the expression turns false at some point
let x = 1;

while (x <= 10) {
  console.log('Hello World!');
  x++;
}
